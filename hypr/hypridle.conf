general {
    lock_cmd = pidof hyprlock || hyprlock    # dbus/sysd lock command (loginctl lock-session)
    unlock_cmd = pkill -f "kitty.*cmatrix-lock"        # same as above, but unlock
    before_sleep_cmd = loginctl lock-session # command ran before sleep
    after_sleep_cmd = hyprctl dispatch dpms on  # command ran after sleep
    ignore_dbus_inhibit = false              # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
    ignore_systemd_inhibit = false           # whether to ignore systemd-inhibit --what=idle inhibitors
}

listener {
    timeout = 300                            # 5 minutes
    on-timeout = loginctl lock-session   # command to run when timeout has passed
    
}

listener { 
    timeout = 2700	# 45 minutes
    on-timeout = systemctl suspend	# So I don't lose battery
}

